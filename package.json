{
  "name": "pathfinder",
  "version": "1.0.1",
  "description": "eDSL for URL parsing",
  "main": "main.elm",
  "scripts": {
    "doc": "elm-make --docs=documentation.json",
    "publish": "elm-package publish",
    "install": "elm-package install --yes",
    "elm": "elm",
    "test": "elm-test --watch"
  },
  "author": "airtucha",
  "license": "ISC",
  "dependencies": {
    "elm": "^0.18.0"
  },
  "devDependencies": {
    "elm-test": "^0.18.12",
    "nodemon": "^1.12.1"
  }
}
