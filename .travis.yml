sudo: false

language: node_js
node_js: '8'

cache:
  directories:
    - elm-stuff/build-artifacts
    - elm-stuff/packages
    - tests/elm-stuff/build-artifacts
    - tests/elm-stuff/packages
    - node_modules

os:
  - linux

install:
  - npm install
  - cd tests
  - npm install
  - cd ..

script:
  - npm run ci:test